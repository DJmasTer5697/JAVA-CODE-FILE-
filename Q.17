Write a java program to find the result sheet of a student using Interfaces. 

interface Student {
    void getDetails(int rollNo, String name);
}

interface Marks {
    void getMarks(int m1, int m2, int m3);
}

class Result implements Student, Marks {
    int rollNo, m1, m2, m3, total;
    String name;
    double percentage;

    public void getDetails(int rollNo, String name) {
        this.rollNo = rollNo;
        this.name = name;
    }

    public void getMarks(int m1, int m2, int m3) {
        this.m1 = m1;
        this.m2 = m2;
        this.m3 = m3;
        total = m1 + m2 + m3;
        percentage = total / 3.0;
    }

    void displayResult() {
        System.out.println("Roll No: " + rollNo);
        System.out.println("Name: " + name);
        System.out.println("Marks: " + m1 + ", " + m2 + ", " + m3);
        System.out.println("Total: " + total);
        System.out.println("Percentage: " + percentage + "%");
        if (percentage >= 40)
            System.out.println("Result: Pass");
        else
            System.out.println("Result: Fail");
    }
}

public class Main {
    public static void main(String[] args) {
        Result r = new Result();
        r.getDetails(101, "Nitesh Kumar");
        r.getMarks(75, 65, 80);
        r.displayResult();
    }
}
